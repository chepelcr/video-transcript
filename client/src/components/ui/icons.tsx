import { Loader2, User, FileText, Menu, Home, LogOut } from "lucide-react";

export const Icons = {
  spinner: Loader2,
  user: User,
  fileText: FileText,
  menu: Menu,
  home: Home,
  logout: LogOut,
};